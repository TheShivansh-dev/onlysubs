services:
  # ── Web admin panel ──────────────────────────────────────────────────────────
  - type: web
    name: onlysub-admin
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn web.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false          # set in Render dashboard
      - key: JWT_SECRET
        sync: false
      - key: OWNER_EMAIL
        sync: false          # owner login email
      - key: OWNER_PASSWORD
        sync: false          # owner login password
      - key: BOT_TOKEN
        sync: false

  # ── Telegram bot worker ───────────────────────────────────────────────────────
  - type: worker
    name: onlysub-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: BOT_TOKEN
        sync: false
